import styles from './Footer.module.css';

function Footer() {
    return (
        <footer className={styles.footerContainer}>
            <p>This is the Footer! Contact me!</p>
        </footer>
    )
}

export default Footer;
